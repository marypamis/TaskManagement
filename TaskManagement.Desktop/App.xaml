<Application x:Class="TaskManagement.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:TaskManagement.Desktop.Helpers"
             xmlns:viewModels="clr-namespace:TaskManagement.Desktop.ViewModels"
             xmlns:views="clr-namespace:TaskManagement.Desktop.Views"
             Startup="Application_Startup">
    <Application.Resources>
        <helpers:StringNotEmptyToVisibilityConverter x:Key="StringNotEmptyToVisibilityConverter"/>

        <DataTemplate DataType="{x:Type viewModels:LoginViewModel}">
            <views:LoginView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type viewModels:DashboardViewModel}">
            <views:DashboardView />
        </DataTemplate>
    </Application.Resources>
</Application>
