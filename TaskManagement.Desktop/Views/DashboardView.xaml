<UserControl x:Class="TaskManagement.Desktop.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <Button Content="Logout"
                    Command="{Binding LogoutCommand}"
                    DockPanel.Dock="Right"
                    Padding="10,6"/>
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    DockPanel.Dock="Right"
                    Padding="10,6"
                    Margin="0,0,8,0"/>
            <TextBlock Text="Task Dashboard"
                       FontSize="22"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="{Binding StatusMessage}" Foreground="Gray"/>
            <TextBlock Text="{Binding ErrorMessage}"
                       Foreground="DarkRed"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyToVisibilityConverter}}"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Tasks}"
                  SelectedItem="{Binding SelectedTask}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Created By User Id" Binding="{Binding CreatedByUserId}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="3"
                Content="Mark Selected as Completed"
                Command="{Binding CompleteTaskCommand}"
                HorizontalAlignment="Left"
                Margin="0,12,0,0"
                Padding="12,6"/>
    </Grid>
</UserControl>
